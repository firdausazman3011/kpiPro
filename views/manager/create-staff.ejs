<div class="row mb-4">
    <div class="col-md-12">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                <h5 class="mb-0 fw-bold">Add New Staff</h5>
            </div>
            <div class="card-body p-4 p-md-5">
                <% if (typeof error !== 'undefined' && error.length > 0) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <form action="/manager/staff/create" method="POST" id="createStaffForm">
                    <div class="mb-3">
                        <label for="name" class="form-label">Name</label>
                        <input type="text" class="form-control form-control-lg" id="name" name="name" required>
                    </div>
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control form-control-lg" id="email" name="email" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control form-control-lg" id="password" name="password" required>
                    </div>
                    <div class="mb-3">
                        <label for="organization" class="form-label">Organization</label>
                        <input type="text" class="form-control form-control-lg" id="organization" name="organization" value="<%= user.organization %>" readonly>
                    </div>
                    <div class="mb-3">
                        <label for="department" class="form-label">Department</label>
                        <select class="form-select form-select-lg" id="department" name="department" required>
                            <option value="">Select Department</option>
                            <% departments.forEach(department => { %>
                                <option value="<%= department.name %>"><%= department.name %></option>
                            <% }); %>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="role" class="form-label">Role</label>
                        <select class="form-select form-select-lg" id="role" name="role" required>
                            <option value="">Select Role</option>
                            <option value="staff">Staff</option>
                            <option value="manager">Manager</option>
                        </select>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary btn-lg rounded-pill px-4">Create Staff</button>
                        <a href="/manager/staff" class="btn btn-secondary btn-lg rounded-pill px-4">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
document.getElementById('createStaffForm').addEventListener('submit', function(e) {
    // Client-side validation for password (if needed, otherwise rely on server)
    // For this example, we'll keep it simple and just focus on UI consistency.
    // If you need specific client-side validation logic, please let me know.
    // The original script didn't have specific validation, so removing `validateForm` call

    // Example of how to show an alert if needed (similar to other forms)
    // const someCondition = false; // Replace with actual validation logic
    // if (someCondition) {
    //     e.preventDefault();
    //     const errorMessage = document.createElement('div');
    //     errorMessage.className = 'alert alert-danger alert-dismissible fade show mt-3';
    //     errorMessage.setAttribute('role', 'alert');
    //     errorMessage.innerHTML = 'Validation error message here<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>';
        
    //     const form = document.getElementById('createStaffForm');
    //     form.parentNode.insertBefore(errorMessage, form);
        
    //     setTimeout(() => {
    //         errorMessage.remove();
    //     }, 5000);
    // }
});
</script> 